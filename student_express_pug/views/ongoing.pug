extends layout

block content 
    .row
    each x in allOngoing 
        .container.col-sm 
        h3 #{x.name}
        ul
            li Department: #{x.dept}
            li Remaining Spots: #{x.remaining}
        p.description #{x.desc}

        h5 Course Id: #{x.id}
        hr
