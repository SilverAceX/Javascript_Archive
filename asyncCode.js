var a = 2;
const doge = 10;
let whatever = 12;

shortFunc = (name, age) => {
    console.log(`This random person is named ${name} and they are ${age}`);
} 

let delay = setTimeout(shortFunc,1000,"Tertius",18);


console.log(a);
delay;
console.log(doge);